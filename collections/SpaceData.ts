import { CollectionConfig } from "payload";

export const SpaceData: CollectionConfig = {
    slug: "spaceData",
    fields: [
        {
            name: 'name',
            type: 'text',
            required:true
        },
    ]
}